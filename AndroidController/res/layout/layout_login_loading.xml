<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" 
    android:gravity="center_horizontal">
    
    <ProgressBar 
        android:layout_width="@dimen/login_loading_width"
        android:layout_height="@dimen/login_loading_height"
        android:layout_marginTop="@dimen/login_loading_progress_margin_top"
        android:indeterminate="false"
        android:indeterminateDrawable="@anim/login_seraching_anim"/>
    <TextView 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center_horizontal"
        android:text="@string/login_loading"
        android:textColor="@color/login_list_item_color"
        android:textSize="@dimen/login_loading_text_size"/>

</LinearLayout>
